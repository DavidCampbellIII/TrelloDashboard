{
  "name": "trello-dashboard-visualizer",
  "version": "1.0.0",
  "description": "A lightweight web-based dashboard for visualizing the progress of a Trello board",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "serve": "serve frontend",
    "serve:dist": "serve frontend/dist",
    "start": "npm run build && npm run serve",
    "start:dist": "npm run build && npm run serve:dist"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "firebase": "^10.8.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "serve": "^14.2.1",
    "typescript": "^5.4.2"
  }
}
